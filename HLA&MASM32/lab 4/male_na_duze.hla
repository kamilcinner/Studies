program male_na_duze;
#include("stdlib.hhf");

static
    c:  char;

begin male_na_duze;

    stdout.put("Wprowadz litere: ");
    stdin.getc();

    if (al >= 'a') then
        if (al <= 'z') then
            sub(32, al);
        endif;
    endif;

    stdout.put("Wprowadzona litera po konwersji do wielkiej litery to: '");
    stdout.putc(al);
    stdout.put("'", nl);

end male_na_duze;